<%= form_for(es2) do |f| %>
  <% if es2.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(es2.errors.count, "error") %> prohibited this es2 from being saved:</h2>

      <ul>
      <% es2.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :contrato_id %>
    <%= f.text_field :contrato_id %>
  </div>

  <div class="field">
    <%= f.label :linea_id %>
    <%= f.text_field :linea_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
